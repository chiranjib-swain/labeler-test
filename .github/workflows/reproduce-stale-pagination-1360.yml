name: Reproduce stale PR pagination issue 1360

on:
  workflow_dispatch:

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  reproduce:
    runs-on: ubuntu-latest
    steps:
      - uses: chiranjib-swain/stale@f30c9dcef6cc0603d2cc51ce00be5ca0e8f6ccd9
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          only-pr-labels: pagination-1360
          exempt-pr-labels: pagination-1360-exempt
          stale-pr-label: U:Stale
          stale-pr-message: ''
          close-pr-message: ''
          close-pr-label: ''
          days-before-pr-stale: 30
          days-before-pr-close: 0
          days-before-issue-stale: -1
          days-before-issue-close: -1
          operations-per-run: 1000
          sort-by: created
          ascending: false