name: Test pagination 1360 - operations-per-run caching (no staling, fixed branch)

on:
  workflow_dispatch:
    inputs:
      operations-per-run:
        description: 'operations-per-run value for this dispatch'
        default: '2'
        required: false

permissions:
  contents: read
  issues: write
  pull-requests: write
  actions: write

jobs:
  cache-test:
    runs-on: ubuntu-latest
    steps:
      - uses: chiranjib-swain/stale@e1f194b1565fb80a45b28a435ec2a8c45909f2bc
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          debug-only: false
          only-pr-labels: pagination-1360
          days-before-pr-stale: -1
          days-before-issue-stale: -1
          operations-per-run: ${{ github.event.inputs.operations-per-run }}
          sort-by: created
          ascending: false
